<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itver.donapps">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitys.HomeActivity">
        </activity>
        <activity android:name=".activitys.MainActivity" />
        <activity
            android:name=".activitys.LoginActivity"
            android:parentActivityName=".activitys.MainActivity" />
        <activity
            android:name=".activitys.RegisterActivity"
            android:parentActivityName=".activitys.MainActivity" />
        <activity
            android:name=".activitys.ProfileUpdateActivity"
            android:parentActivityName=".activitys.HomeActivity" />
        <activity android:name=".activitys.ListDonorsFoundActivity" />

        <activity android:name=".activitys.SplashActvity"
                  android:theme="@style/splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>